workers = 4  # Adjust the number of workers as per your requirements
threads = 2  # Adjust the number of threads per worker as per your requirements
bind = '0.0.0.0:8000'  # Change the IP address and port as needed